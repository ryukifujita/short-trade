# 20. 実装状況（Phase 1 / 作業3の途中経過）

`docs/16` §16.6 の「3. 単体バックテスト」に向けて、検証基盤を実装した。

> **重要**: 本章の数字はすべて**合成データ**によるものであり、戦略の成績とは無関係である。
> 実データでの検証は、J-Quants からのデータ取得後に行う（§20.5）。

---

## 20.1 実装したもの

| モジュール | 役割 |
|---|---|
| `src/short_trade/indicators.py` | 指標エンジン。`catalog/schema.md` の記法を実装。**バックテストと実運用で共有する**（VR-001） |
| `src/short_trade/spec.py` | 仕様ファイルの読み込みと検証。損切り未設定の仕様を起動時に弾く（DEC-004） |
| `src/short_trade/backtest.py` | イベント駆動バックテスタ。当日終値判定 → 翌営業日始値の成行執行 |
| `src/short_trade/jquants.py` | J-Quants クライアントとローカルキャッシュ。**未疎通**（§20.4） |
| `src/short_trade/setup_ui.py` | **セットアップ画面**（127.0.0.1 のみ）。認証情報の入力 → 接続確認 → `.env` 保存 → 取得 → ST-06 基準線 → `data/setup_report.json` |
| `src/short_trade/detectors.py` | 式で書けないパターン検出器（VCP）。因果性をテストで固定 |
| `src/short_trade/correlate.py` | 戦略間の相関測定（VR-045） |
| `src/short_trade/cli.py` | `setup` / `selfcheck` / `smoke` / `fetch` / `backtest` / `compare` / `correlate` |
| `tests/` | 75件。ルックアヘッド検査5件、レビュー回帰16件（`docs/21`）、VCP 5件、相関3件、セットアップE2E ほか |

### 検証基盤の設計で守ったこと

| 要求 | 実装 |
|---|---|
| VR-001 二重実装の禁止 | 条件式は `catalog/rules/*.yaml` にしかない。コードは式を解釈するだけ |
| VR-002 ルックアヘッド防止 | 約定価格は必ず**翌営業日の始値**。判定日の終値では約定しない |
| DEC-004 / RM-001 損切り必須 | 損切り価格が決まらない候補はエントリーしない。仕様の時点でも弾く |
| RM-001a 論理ストップ | 終値がストップを割ったら**翌執行枠で成行**。逆指値は使わない |
| RM-001b 想定損失2倍 | サイズ計算は `(建値 − 損切り) × 2` を想定損失として扱う |
| FR-C07 選抜外の戦略は稼働不可 | `selfcheck` が `status: selected_*` を確認する |
| VR-011 スリッページ | 片道 0.10% を既定（成行のため保守的に） |
| PL-002 コスト | 手数料 0%（ゼロ革命）。料率は設定で変更可能 |

---

## 20.2 テストが見つけた仕様のバグ 2件

**基盤を作った最初の価値は、成績ではなくバグの検出だった。**

| # | バグ | 内容 | 修正 |
|---|---|---|---|
| 1 | `common.yaml` のレジーム条件が銘柄の終値を見ていた | `condition: "close > SMA(close, 200)"` と書いていたが、`close` は評価時に**当該銘柄の終値**に束縛される。TOPIX を見ているつもりで、実際は個別銘柄の200日線を見ていた | `INDEX.close > SMA(INDEX.close, 200)` に修正。`schema.md` に「指数を参照するときは必ず `INDEX.` を付ける」と明記 |
| 2 | `ST-04` が未定義の `return_126d` を参照していた | 相対強度の条件 `PCTRANK(return_126d, universe) >= 70` を書きながら、`return_126d` の定義がなかった | `definitions` に `close / close[-126] - 1` を追加 |

バグ1は**全戦略に影響する重大なもの**だった。書き下しただけでは気づかず、実行して初めて分かった。

---

## 20.3 現在動く戦略と、動かない戦略

`python -m short_trade smoke` の結果（合成データ）。

| ID | 状態 | 内容 |
|---|---|---|
| **ST-06** ドンチャン | **動く** | Phase 2 の基準線 |
| **ST-09** モメンタム | **動く** | クロスセクショナル順位を実装して解決 |
| **ST-12** RSI2 | **動く** | — |
| **ST-16** 移動平均乖離 | **動く** | — |
| **ST-38** NR7 | **動く** | 合成データでは条件が成立しない（データの性質による。異常ではない） |
| **ST-04** VCP | **動く** | 因果的ジグザグの検出器を実装（`docs/25`） |
| ST-23 PEAD | **未実装** | 決算データが必要。加えて `definitions.event_day` が説明文のままで式になっていない |
| **ST-35** ステージ分析 | **動く** | `stage_1_days` を `STREAK` で定義（`docs/25`） |

> **Phase 2 の4本はすべて動く。** 残る未実装は ST-23 のみ（四半期EPSの取得が必要）。

未実装は**黙って無視せず、名前を挙げて落とす**設計にした。
シグナルが出ないだけだと「効かない戦略」に見えてしまい、検証結果を誤読するためである。

---

## 20.4 J-Quants クライアント（V2 APIキー方式）

当初は公開情報からエンドポイントを推定して自前実装していたが、
**実際に接続したところ V1 の認証方式ごと廃止されていた**（HTTP 403）。
経緯は [`docs/23_JQuants_V2移行.md`](23_JQuants_V2移行.md)。

現在は **JPX 公式クライアント `jquants-api-client`（`ClientV2`）を土台**にしている。
`src/short_trade/jquants.py` は薄いラッパで、内部形式への正規化とキャッシュだけを持つ。
エンドポイント・パラメータ名・ページングを自前で持たないため、仕様変更に強い。

### 認証情報の渡し方

```bash
export JQUANTS_API_KEY="..."      # ダッシュボードで発行したAPIキー
```

`python -m short_trade setup` の画面から入力すれば `.env` に保存される。

**リポジトリには絶対に置かない**（NFR-020 / `.gitignore` で `config/secrets.yaml` と `.env` を除外済み）。

---

## 20.5 次にやること

| 順序 | 作業 | 備考 |
|---|---|---|
| 1 | J-Quants で U-1〜U-5 を実測（`docs/19` §19.7） | 遡れる期間・TOPIX の可否・レートリミット |
| 2 | `fetch` を実行してデータを揃える | 最初は数銘柄で疎通確認。列名を目視 |
| 3 | **ST-06 の実データ単体バックテスト** | ドンチャンの既知の挙動を再現するか。**成績より基盤の正しさが目的** |
| 4 | ~~VCP 検出器の実装~~ | 済（`docs/25`） |
| 5 | ~~ST-04 / ST-09 / ST-12 の単体バックテスト~~ | 済（`docs/26`）。ST-04 は標本不足で判定不能 |
| 6 | ~~4本の相関行列~~ | 済（`docs/26` §26.3）。判定は残差相関に変更（DEC-066） |
| 7 | ユニバース拡張をランチャーに組み込む | ST-04 の標本を作る |
| 8 | ST-23（PEAD）の実装 | 四半期EPSの取得とイベント系列 |
| 9 | 等ウェイト合成のバックテスト（複数戦略を1つの資金で同時運用） | BR-004「合成 vs 最良単一」の判定。リスク率と決算跨ぎ方針（RM-072）の最終決定もここで |
| 10 | ~~300 銘柄での ST-04 判定~~ | 済（`docs/29` §29.2）。既定値で G1 不合格。損切り幅の探索は分割の実装後 |
| 11 | ~~学習／検証の期間分割と、宣言済みパラメータの 1 回限りの探索~~ | 済（`optimize`、`docs/30` §30.2）。結果待ち |
| 12 | ~~時点ユニバースでの再測定~~ | 済（`docs/30`）。5 本とも G1 未達 |
| 13 | 等ウェイト合成のバックテスト | BR-004。探索結果を見てから |

---

## 20.6 使い方

```bash
pip install -r requirements.txt

# 仕様ファイルの検査（ネットワーク不要）
PYTHONPATH=src python -m short_trade selfcheck

# 合成データで基盤の健全性を確認（ネットワーク不要）
PYTHONPATH=src python -m short_trade smoke --strategy ST-06

# テスト
python -m pytest tests/ -q

# データ取得（要 J-Quants 認証情報）
PYTHONPATH=src python -m short_trade fetch --codes 7203,6758 --start 2015-01-01

# バックテスト
PYTHONPATH=src python -m short_trade backtest --strategy ST-06

# リスク率×スリッページの比較（Phase 2 の4本）、相関、条件ファネル
PYTHONPATH=src python -m short_trade compare
PYTHONPATH=src python -m short_trade correlate
PYTHONPATH=src python -m short_trade funnel --strategy ST-04
```
